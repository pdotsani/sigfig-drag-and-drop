# SigFig: Image drag and drop  
  
# Summary  
  
Build dist: `npm run build`  
Build and run: `npm run buildandstart`  
Run without build: `npm run start` or open `dist/index.html` in a web browser  